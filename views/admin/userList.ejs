<!DOCTYPE html>
<html lang="en">


<%- include("../partials/headProduct.ejs") %>

    <body>
        <%- include("../partials/headerAdmi.ejs") %>
            <main>

                <h2 class=list>USUARIOS</h2>
                <div class="contenedor">
                    <% users.forEach(user=> {%>
                        <div class="card">
                            <div class="card-contenedor-img">
                                <img src="/img/<%= user.img %>" alt="Avatar">
                            </div>
                            <div class="container">
                                <div class="card-contendor-titulo">
                                    <h2 class="lugar">
                                        <%= user.user==undefined ? user.email : user.user %>
                                    </h2>
                                    <h4 class="lugar">
                                        <%= user.category %>
                                    </h4>
                                </div>
                                <div class="buttonIn">
                                    <form method="post" action='/userDelete/<%= user.id%>?_method=delete'>
                                        <button id="clear3" type="submit">Eliminar</button>
                                    </form>
                                    <a href="/userEdit/<%= user.id %>"><button id="edit3">Editar <i
                                                class="fas fa-edit"></i></button></a>

                                </div>
                            </div>
                        </div>
                        <% }) %>
                </div>
            </main>
            <script> hamburger = document.querySelector(".hamburger");
                hamburger.addEventListener("click", function () {
                    document.querySelector("body").classList.toggle("active");
                })</script>
            <script type="text/javascript">
                function redirect() {

                    window.location.href = "/edit/"
                }
            </script>
    </body>

</html>